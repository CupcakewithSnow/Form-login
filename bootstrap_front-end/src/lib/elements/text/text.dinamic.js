import styled from 'styled-components'
import { THEME_SIZE, THEME_COLOR, spacing } from '../../theme'

export const TextDinamic = styled.span`
    font-size: ${THEME_SIZE.FONT.SMALL};
    color: ${THEME_COLOR.BLACK};
    padding:${spacing(2)};
`; 